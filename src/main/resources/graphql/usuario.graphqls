type Usuario {
    id: ID!
    nombre: String
    contrasenha: String
}
input InputUsuario {
    nombre: String!
    contrasenha: String!
}
type PaginadorUsuario {
    items: [Usuario!]!
    totalItems: Int!
    totalPages: Int!
    currentPage: Int!
    itemsCount: Int!
}