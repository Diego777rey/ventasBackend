package py.edu.facitec.ventas.enums;
//la funcion de este enum es definir roles posibles que el usuario puede tener en mi apliacion
public enum Rol {
    ADMIN,
    USUARIO,
    EDITOR
}
//los enum se utilizan para evitar errores al usar cadenas y en casos donde quieras un conjunto limitado y predefinido de valores